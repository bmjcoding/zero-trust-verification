.DS_Store
.claude/settings.local.json
.ruff_cache/
tests/codebase-health/audit/
tests/codebase-health/test-fixtures/blind/
.venv/
__pycache__/
*.pyc
# the runtime outcome store at the repo root only — NOT the vendored schema copies
# under schema/outcome/ or plugins/*/schema/outcome/ (anchored with a leading slash).
/outcome/
